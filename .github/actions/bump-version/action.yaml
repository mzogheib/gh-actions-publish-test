name: 'Bump version'

description: 'Bumps the major, minor or patch version'

inputs:
  type:
    description: 'The type of version to bump'
    required: true

runs:
  using: composite
  steps:
    - run: echo "npm version ${{ inputs.type }}"
      shell: bash
