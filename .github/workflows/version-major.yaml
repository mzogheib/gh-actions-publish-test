name: Version Major Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Bump the major version
        uses: ./.github/actions/bump-version
        with:
          type: "major"
